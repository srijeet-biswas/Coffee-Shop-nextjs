import React from "react";
export default function LocalityLayout({ children }: Readonly<{children: React.ReactNode;}>) {
    return (
        <React.Fragment>
            {children}
        </React.Fragment>
    )
}